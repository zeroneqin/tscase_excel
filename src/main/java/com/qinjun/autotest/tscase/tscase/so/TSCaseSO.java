package com.qinjun.autotest.tscase.tscase.so;

public class TSCaseSO {
    private final static Logger logger = LoggerFactory.getLogger(TSCaseSO.class);

    public static Response sendRequest(String url, String req) {

    }
}
